import { type Config } from "prettier";

const config: Config = {
  trailingComma: "all",
  semi: false,
  printWidth: 80,
  singleQuote: false,
  useTabs: false,
  tabWidth: 2,
};

export default config;
